/**
 * Starts the API defined in backend/server.js from the repo root.
 * Dependencies live in backend/node_modules — install with:
 *   cd backend && npm install
 * Then from repo root: node server.js
 * Or: cd backend && npm start
 */
require('./backend/server.js');
